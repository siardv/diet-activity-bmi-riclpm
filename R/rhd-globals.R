utils::globalVariables(c(
  ".",
  # describe()
  "vars", "variable", "n", "mean", "min", "max", "sd",
  "skew", "kurtosis", "se", "normality", "p_value",
  # compare_ses_groups()
  "op", "lhs", "rhs", "group", "est", "pvalue", "effect", "label",
  "lagged_effect", "z",
  # model_fit_table()
  "Component", "Fit", "Parameter", "Group", "SES",
  # table_estimates()
  "lagged_effects_only"
))
